# Runtime deps for the Fidolio API (exactly what backend/api imports).
# Kept slim so cloud builds are fast and reliable. The heavy ML/data libs
# (used only by one-off local enrichment scripts) live in requirements-dev.txt.
fastapi==0.111.0
uvicorn==0.30.1
spotipy==2.23.0
psycopg2-binary==2.9.10
python-dotenv==1.0.1
requests==2.32.3
